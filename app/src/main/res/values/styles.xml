<resources>

    <style name="AddHabitItem" parent="">
        <item name="android:background">@color/colorWhite</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">9dp</item>
        <item name="android:padding">27dp</item>
    </style>

    <style name="AddDayButton" parent="">
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:textColor">@color/colorMainGray</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:background">@drawable/day_circlr_button_selector</item>
    </style>

    <style name="myTimePicker" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:divider">@color/colorMain</item>
    </style>

</resources>
